# React User Dashboard

Minimal Vite + React + TypeScript starter scaffold for the demo dashboard.

Quick start:

```bash
npm install
npm run dev
```
# react_node_poc_bmad_framework
This repo is for POC work
